import ScrollView from "@/components/ScrollView";
import React from "react";
import { Text } from "react-native-paper";

export default function Local(searchQuery: string) {
  return (
    <ScrollView>
      <Text>Local</Text>
    </ScrollView>
  );
}
